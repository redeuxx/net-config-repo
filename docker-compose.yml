version: '3.8'

services:
  backend:
    build: ./backend
    volumes:
      - ./data:/app/data
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////app/data/devices.db

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend